---
name: unfreeze
description: |
  Clear the freeze boundary set by /dk-harness freeze, allowing edits to all
  directories again. Use when you want to widen edit scope.
---

# /dk-harness unfreeze — Remove Edit Restriction

Clears the directory freeze set by `/dk-harness freeze`.

## Usage
```
/dk-harness unfreeze
```

After unfreezing, Edit and Write operations are allowed on all project files.
